#include<iostream>
#include<string>
using namespace std;

void printZ()
{
    cout<<"*****"<<endl;
    cout<<"   *"<<endl;
    cout<<"  *"<<endl;
    cout<<" *"<<endl;
    cout<<"*****";
}

/*int maximumEle(vector<int>& arr)
{
    int maxEle=-1e8;
    for(int i=0;i<arr.size();i++)
    {
        if(arr[i]>maxEle)
        {
            maxEle=arr[i];
        }
    }
    return maxEle; 
}

int minimumEle(vector<int>& arr)
{
    int minEle=1e8;
    for(int i=0;i<arr.size();i++)
    {
        if(arr[i]<minEle)
        {
            minEle=arr[i];
        }
    }
    return minEle;
}

int findEle(vector<int> & arr,int data)
{
    int idx=-1;
    for(int i=0;i<arr.size();i++)
    {
        if(arr[i]==data)
        {
            idx=i;
            break;
        }
    }
    return idx;
}

int findFirst(vector<int>& arr,int data)
{
    int idx=-1
    for(int i=0;i<arr.size();i++)
    {
        if(a[i]==data)
        {
            idx=i;
            break;
        }

    }
    return idx;
}

int findLast(vector<int>& arr,int data)
{
    for(int i=(arr.size-1);i>0;i--)
    {
        if(a[i]==data)
        {
            idx=i;
            break;
        }
    }
    return idx;
}

int 2d()

int main()
{
    int n;
    cin>>n;
    vector<int> arr(n);
    for(int i=0;i<arr.size();i++)
    {
        cin>>arr[i];
    }
    int d;
    cin>>d;
    cout<<maximumEle(arr)<<endl;
    cout<<findEle(arr,d)<<endl;
    cout<<minimumEle(arr)<<endl;
    cout<<findFirst(arr,d)<<endl;
    cout<<findLast(arr,d)<<endl;

} */

int main()
{
    printZ();
return 0;
}